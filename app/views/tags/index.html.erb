<h2>タグ一覧</h2>
<% @tags.each do |tag| %>
  <%= link_to tag_path(tag.name) do %>
    <span>・<%= tag.name %></span>
  <% end %>
<% end %>

<%= link_to tags_rank_path do %>
  <h3>人気のタグランキングはこちら</h3>
<% end %>
